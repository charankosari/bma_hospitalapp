import { SafeAreaView, Text, ScrollView } from "react-native";
import React from "react";

export default function AddDoctors() {
  return (
    <SafeAreaView>
      <ScrollView>
        <Text>AddDoctors</Text>
      </ScrollView>
    </SafeAreaView>
  );
}
